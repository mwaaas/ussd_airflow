machine:
  services:
    - docker


dependencies:
  override:
    - docker info
    - docker-compose build


test:
  override:
    - docker-compose up
    - curl --retry 10 --retry-delay 5 -v http://localhost:9000